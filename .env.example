# Optional runtime overrides. Copy to `.env` and adjust as needed.
# The application reads these from the process environment.

VOICE=en-US-ChristopherNeural
FPS=24
VIDEO_WIDTH=1080
VIDEO_HEIGHT=1920
INPUT_FILE=input_scripts.json
OUTPUT_DIR=output
TEMP_DIR=temp
LOG_LEVEL=INFO
